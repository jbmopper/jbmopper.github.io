/**
 * Stub for Cloudflare Turnstile integration.
 * Returns null until a real site key is configured via PUBLIC_TURNSTILE_SITE_KEY.
 */
export async function getTurnstileToken(): Promise<string | null> {
  return null;
}
