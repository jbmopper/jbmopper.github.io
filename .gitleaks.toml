title = "jbmopper.github.io gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Observable generated chart key false positives"
stopwords = [
  "pct_survived_200",
  "pct_survived_600",
]
